{
  "name": "stock-monitor-functions",
  "version": "1.0.0",
  "description": "Netlify Functions for stock monitoring",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "mysql2": "^3.6.0"
  },
  "devDependencies": {
    "netlify-lambda": "^2.0.15"
  }
}
